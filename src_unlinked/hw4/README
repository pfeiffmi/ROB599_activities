1. Unzip the two packakes (hw4 and hw4_interfaces) into your workspace

2. Open a new terminal with the working directory set to your ROS2 workspace

3. Run `colcon build` in your ROS2 workspace

4. Run `source install/setup.bash`

5. Run `ros2 launch hw4 wave.py` to test the wave generator

6. Run `ros2 launch hw4 launch_rocket_without_cancel.py` to test the rocket launch without the cancellation

7. Run `ros2 launch hw4 launch_rocket_with_cancel.py` to test the rocket launch with the cancellation